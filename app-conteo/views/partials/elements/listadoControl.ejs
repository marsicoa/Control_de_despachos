<p>Resumen de ejecucion <% const fyh=new Date().toLocaleString() %>
        <%= fyh %>
</p>
<table class="table" id="tablaListado">
    <thead style="background-color: bisque; color:black">
        <tr>
            <th style="text-align: center;">Bulto</th>
            <th style="text-align: center;">Cantidad</th>
        </tr>
    </thead>
    <tbody id="tablalistado">
        <% if (listado.length !==0) { %>
            <% listado.forEach((element)=> { %>
                <tr id=<%=element.value.code %>>
                    <td style="text-align: center;">
                        <%=element.value.code%>
                    </td>
                    <td style="text-align: center;">1</a></td>
                </tr>
                <% }) %>
                    <% } else { %>
                        <tr>
                            <td style="text-align: right;"></td>
                            <td style="text-align: left;">No se han cargado bultos</td>
                        </tr>
                        <% } %>
    </tbody>
    <tfoot>
        <tr style="background-color: rgb(160, 84, 84);">
            <td style="text-align: center; color: rgb(0, 0, 0);">
                <h5>Total</h3>
            </td>
            <td style="text-align: center; color: rgb(0, 0, 0);">
                <h5>
                    <%= listado.length %>
                        </h3>
            </td>
        </tr>
    </tfoot>
</table>
<a href="https://app.sumed.local.com:8080/control"><b style="margin-top: 10%;">Volver</b></a>
<script>window.print()</script>